
// Simple script to ensure WASM files are properly included in the build
// This would typically be run as part of a build script in package.json
console.log("WASM files should be copied to the public folder for production builds");
// The actual copying would be handled by a build script
